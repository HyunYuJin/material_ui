import type {
  ButtonType
} from '../types'

class Button {
  public config: ButtonType

  constructor (
    config: ButtonType
  ) {
    this.config = config
  }

  classList () {

  }

  clickHandler () {

  }

  render () {

  }
}

export default Button